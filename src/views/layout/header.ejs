<header>
    <h1><a href="/">my project with EJS</a></h1>
    
    <nav>
        <a href="/product">products</a>

        <% if (!user) { %>
          <a href="/authentication/sign-in">sign in</a>
        <% } %>

        <% if (user) { %>
          <a href="/authentication/sign-out">Log out</a>
        <% } %>
    </nav>
</header>